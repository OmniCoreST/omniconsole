# Moxa ioThinx 45MR-2404 — 4-channel Relay Output
#
# Source: ioThinx 4510 Series User's Manual V2.1, August 2019, Appendix A
# Protocol: Modbus/TCP via ioThinx 4510 adapter, port 502
#
# IMPORTANT: Addresses are MODULE-RELATIVE. Actual PDU address = slot_base + relative_address.
# Slot base depends on which slot this module occupies in the ioThinx 4510 chassis.
#
# FC01 (coil R/W): relayStatus ch0-3 (4 BOOL, 0=OFF 1=ON),
#   relayCurrentCountReset ch0-3 (4 BOOL, W=1 to reset current count)
# FC03 (holding R/W): see [[registers]] below
# FC04 (input R): relayTotalCount ch0-3 (u64, 4 regs/ch = 16 regs total),
#   relayCurrentCount ch0-3 (u64, 4 regs/ch = 16 regs total)

vendor = "Moxa"
model = "ioThinx 45MR-2404"
description = "4-relay output ioThinx 45MR slot module. Addresses are module-relative; actual PDU address = slot_base + relative_address. FC01: relayStatus ch0-3 (0=OFF 1=ON), relayCurrentCountReset ch0-3 (W=1 to reset). FC03: relayStatusAll bitmask (rel 0). FC04: relayTotalCount ch0-3 u64 4-words/ch (16 regs), relayCurrentCount ch0-3 u64 4-words/ch (16 regs). Total and current relay cycle counts are 64-bit (4 u16 registers per channel)."

# ===== FC03 Holding Registers (module-relative addresses) =====

[[registers]]
address = 0
type = "u16"
name = "relayStatusAll (bitmask bit0-3=ch0-3; 0=OFF 1=ON; R/W)"
